apiVersion: v1
kind: Pod
metadata:
  labels:
    app.kubernetes.io/name: euboulia-worker
spec:
  restartPolicy: Never
  containers:
    - name: runtime
      # Replaced by target.runtime.expected.container.image from the resolved recipe.
      image: example.invalid/replaced-at-runtime
      command: [sleep, infinity]
      resources:
        limits:
          nvidia.com/gpu: "8"
        requests:
          nvidia.com/gpu: "8"
