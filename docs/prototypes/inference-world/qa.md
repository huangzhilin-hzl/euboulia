# v0.2 验收记录

日期：2026-09-07。只操作本地静态原型；无外部消息、模型调用或 GPU 执行。

## 自动检查

- `node --test tests/web/*.test.mjs`：24 项通过，包括原任务图、结果仪表盘和 5 项新研究室状态测试。
- `node --check`：`room.mjs`、`room-state.mjs` 通过。
- `git diff --check`：通过。

## 浏览器操作

使用应用内 Chromium，桌面布局约 1241 × 818 CSS px，小屏约 354 × 767 CSS px（浏览器缩放使 CSS 尺寸小于 viewport 设置）。

| 实际操作 | 检查结果 |
| --- | --- |
| 打开 Profile，点击等待区间 | 正确显示证据缺口；将窗口引用与问题带入输入区 |
| 勾选交办，发送给 Prism | 新消息携带引用，新任务保留负责人、来源消息和同一证据 |
| 选择补采多 rank | 只新增一次方向任务；工作计数与关系视图同步 |
| 开始、暂停、恢复 GPU 任务 | 状态同步；恢复回到队列，保留上下文 |
| GPU 连接中断，再打开受阻筛选 | GPU 工作显示等待算力；Computer 分析保持进行中 |
| 输入草稿、切换研究室、返回、刷新 | 草稿、消息、任务和引用仍存在 |
| 小屏展开导航、进入空研究室 | 导航可用，输入区完整可见，文档无水平溢出 |
| 空研究室打开上下文 | 显示未绑定模型和算力，不继承 DSV4 的场景条件 |
| 输入以 @Prism 开头的交办 | 负责人自动匹配 Prism，仍可手动改变 |
| 在新研究室引用旧研究室经验并发送 | 引用在新研究室的产物视图可见，来源不变 |
| 搜索“等待” | 能找到相关消息、工作和 Profile |
| 查看控制台 | 未发现警告或错误 |

未实测：真实多人协作、runtime 流式回复、Pod 生命周期、实际 Trace 数据、键盘屏幕阅读器完整遍历及生产级超大消息列表。这些均不属于当前静态原型能力。

验收结束后将本地演示复位，便于从初始场景体验。

## 外观切换补充验收

四套主题在浏览器选择器中即时应用。检查正文、输入区、图表和详情配色；石墨主题刷新后保留，原草稿与证据详情不受切换影响。在外观弹窗按 Esc 仅关闭外观，不关闭已打开的证据详情。

主要文字/背景、辅助文字/侧栏、按钮文字/强调色、提示文字/提示背景、正向指标/指标背景均做了静态配色对比度检查（至少 4.5:1）；这不是完整的可访问性审计。
